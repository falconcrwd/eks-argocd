## Introduction

This repository contains the deployment manifests for Falcon Operator which will be deployed into an EKS cluster via ArgoCD workflows which are in this folder as well.

### Pre-requisites:
- EKS cluster A is already configured with ArgoCD and connected to the Git repository containing Falcon Operator manifest